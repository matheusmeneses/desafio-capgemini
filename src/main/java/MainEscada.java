import java.util.regex.Matcher;
import java.util.regex.Pattern;

public class MainEscada {
    public static void main(String[] args) {
        Escada escada = new Escada(20);
        escada.criar();
    }
}
